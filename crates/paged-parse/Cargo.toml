[package]
name = "paged-parse"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Import parser (ZIP + XML source packages). Produces a typed AST of the supported schema subset."

[lints]
workspace = true

[dependencies]
# N5 — the Paged model types live here (parser depends on the model, imports
# into it). paged-parse re-exports them so its dependents are untouched.
paged-model = { path = "../paged-model" }
thiserror = { workspace = true }
bytes = { workspace = true }
zip = { workspace = true }
quick-xml = { workspace = true }
serde = { workspace = true }
tracing = { workspace = true }
base64 = { workspace = true }

[dev-dependencies]
zip = { workspace = true }
